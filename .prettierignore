# Markdown only; Biome owns every other language.
# `!*/` is required: gitignore cannot re-include a file under an excluded directory.
*
!*/
!**/*.md

dist
coverage
.licenses
logs

# Release note templates, shipped to users as content.
src/actions/drafter/lib/build-release-payload/static
