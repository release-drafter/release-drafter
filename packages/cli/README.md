# @release-drafter/cli

Private workspace containing the Release Drafter CLI runtime. It is bundled
into the public [`release-drafter`](../release-drafter/README.md) package facade
and is not published independently.

Consumers should install or invoke `release-drafter`, not
`@release-drafter/cli`.
