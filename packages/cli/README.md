# @release-drafter/cli

This private workspace package contains the Release Drafter CLI. The build
includes it in the public [`release-drafter`](../release-drafter/README.md)
package. npm does not publish this workspace separately.

Install or run `release-drafter`. Do not install `@release-drafter/cli`.
