export const enum MajorMinorPatch {
	major = 'major',
	minor = 'minor',
	patch = 'patch',
}

export const enum SORT_BY {
	mergedAt = 'merged_at',
	title = 'title',
}

export const enum SORT_DIRECTIONS {
	ascending = 'ascending',
	descending = 'descending',
}
