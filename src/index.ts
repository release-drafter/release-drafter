/**
 * The entrypoint for the action. This file simply imports and runs the action's
 * main logic.
 *
 * Do not add any logic to this file; instead, add it to `src/main.ts`.
 *
 * `src/main.ts` is the entrypoint for tests and should contain all the action's
 * main logic.
 */
import { run } from './main.js'

/* istanbul ignore next */
run()
