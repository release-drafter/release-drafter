import { type Logger } from '../../../../common/index.js';
import type { Config, ExclusiveInput } from '../../config/index.js';
import type { findPreviousReleases } from '../find-previous-releases/index.js';
import type { resolveVersionKeyIncrement } from './resolve-version-increment.js';
type Release = Exclude<Awaited<ReturnType<typeof findPreviousReleases>>['lastRelease'], undefined>;
export declare const getVersionInfo: (params: {
    logger?: Logger;
    lastRelease: Pick<Release, 'tag_name' | 'name'> | undefined;
    config: Pick<Config, 'version-template' | 'tag-prefix' | 'prerelease-identifier'>;
    input: Pick<ExclusiveInput, 'version' | 'tag' | 'name'>;
    versionKeyIncrement: ReturnType<typeof resolveVersionKeyIncrement>;
}) => {
    $NEXT_MAJOR_VERSION: string;
    $NEXT_MAJOR_VERSION_MAJOR: string | null;
    $NEXT_MAJOR_VERSION_MINOR: string | null;
    $NEXT_MAJOR_VERSION_PATCH: string | null;
    $NEXT_MINOR_VERSION: string;
    $NEXT_MINOR_VERSION_MAJOR: string | null;
    $NEXT_MINOR_VERSION_MINOR: string | null;
    $NEXT_MINOR_VERSION_PATCH: string | null;
    $NEXT_PATCH_VERSION: string;
    $NEXT_PATCH_VERSION_MAJOR: string | null;
    $NEXT_PATCH_VERSION_MINOR: string | null;
    $NEXT_PATCH_VERSION_PATCH: string | null;
    $NEXT_PRERELEASE_VERSION: string;
    $NEXT_PRERELEASE_VERSION_PRERELEASE: string | null;
    $RESOLVED_VERSION: string;
    $RESOLVED_VERSION_MAJOR: string | null;
    $RESOLVED_VERSION_MINOR: string | null;
    $RESOLVED_VERSION_PATCH: string | null;
    $RESOLVED_VERSION_PRERELEASE: string | null;
};
export {};
